import greenfoot.*;

public class Test extends Actor
{
    int x = 0;
    int y = 0;
    public void act() 
    { 
           /*GreenfootImage image = getImage();
           image.clear();
           image.drawString("Für Spieler 1 bitte einmal 1 drücken", 20, 80);
            
            x = getX();
            y = getY();
            if(isAtEdge() == true)
            {
                turn(Greenfoot.getRandomNumber(45));
            }
            move(1);*/
    }    
}
